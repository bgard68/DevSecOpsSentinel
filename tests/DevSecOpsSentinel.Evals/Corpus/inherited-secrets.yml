# GHA008 only: the called workflow receives the whole secret store.
name: Inherited secrets
on:
  push:
permissions:
  contents: read
jobs:
  call:
    uses: ./.github/workflows/reusable.yml
    secrets: inherit
