# Security model

Phase B accepts text only and never executes workflow content. Input is limited
to 100 KB. The API does not accept repository URLs, credentials, tokens, file
paths, or shell commands. Findings are generated by deterministic rules.

Automatic patches are deliberately conservative. Unsafe trigger usage receives
a recommendation but is not silently rewritten because changing trigger
semantics can alter intended behavior.
